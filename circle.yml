machine:
  timezone: Asia/Tokyo
  node:
    version: 6.0.0

dependencies:
  override:
    - npm install -g typescript
    - npm install -g tsconfig-cli
    - npm install -g electron-packager
    - npm install

deployment:
  master:
    branch: master
    commands:
      - ~/$CIRCLE_PROJECT_REPONAME/script/build_circleci.sh
     
test:
  override:
    - echo No test, never mind.
